// Unused now that Worlebury runs as a single entity — kept as an empty
// module rather than deleted (this environment doesn't allow file deletion).
// Safe to remove entirely in a future cleanup pass.
export {};
